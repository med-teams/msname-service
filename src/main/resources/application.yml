server:
  port: 8081

spring:
  application:
    name: msname-service
  datasource:
    url: jdbc:h2:mem:msname
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
